<Window
    x:Class="ReportingFixedDocument.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ReportingFixedDocument"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{Binding Path=WindowTitel, FallbackValue=WindowTitel}"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.Icon>
        <BitmapImage UriSource="pack://application:,,,/Resources/Picture/ApplicationIcon.png" />
    </Window.Icon>
    <Grid x:Name="gridMain">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Column="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="24"
            Text="Beispiel zu Fixed Documents (Reporting mit XAML)" />

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <Button
                x:Name="BtnA4QuerClick"
                Width="200"
                Height="30"
                Click="BtnOpenReportClick"
                Content="Fixed Document A4 Quer" />

            <Label Height="10" />

            <Button
                x:Name="BtnA4HochClick"
                Width="200"
                Height="30"
                Click="BtnOpenReportClick"
                Content="Fixed Document A4 Hoch" />
        </StackPanel>

    </Grid>
</Window>
